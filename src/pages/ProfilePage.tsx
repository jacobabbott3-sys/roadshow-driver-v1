import { LogOut, UserRound } from 'lucide-react';import { useAuth } from '../context/AuthContext'
export function ProfilePage(){const {profile,user,signOut}=useAuth();return <main className="page"><header className="page-header"><div><p className="eyebrow">YOUR ACCOUNT</p><h1>Profile</h1><p>Personal details and account access.</p></div></header><section className="profile-card"><div className="profile-avatar"><UserRound size={32}/></div><div><h2>{profile?.full_name||'Roadshow team member'}</h2><p>{user?.email}</p><span className="role-pill">{profile?.role||'driver'}</span></div></section><button className="button danger" onClick={()=>void signOut()}><LogOut size={18}/> Sign out</button></main>}
