import { ChevronDown,HelpCircle } from 'lucide-react';import { useState } from 'react';import { PageState } from '../components/PageState';import { useAsync } from '../hooks/useAsync';import { getResources } from '../lib/driverData'
export function FaqPage(){const q=useAsync(getResources,[]),[open,setOpen]=useState<string|null>(null),faqs=q.data?.filter(r=>r.kind==='faq')||[];return <main className="page"><header className="page-header"><div><p className="eyebrow">ANSWERS</p><h1>Frequently asked questions</h1><p>Quick answers for life on the road.</p></div></header><PageState loading={q.loading} error={q.error} empty={!faqs.length}><section className="resource-section">{faqs.map(r=><article className="resource-row" key={r.id}><button onClick={()=>setOpen(open===r.id?null:r.id)}><HelpCircle/><strong>{r.title}</strong><ChevronDown className={open===r.id?'rotated':''}/></button>{open===r.id&&<p>{r.content}</p>}</article>)}</section></PageState></main>}
