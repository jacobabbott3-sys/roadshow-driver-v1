import { useState, type FormEvent } from 'react';import { ArrowLeft, Mail } from 'lucide-react';import { Link } from 'react-router-dom';import { useAuth } from '../context/AuthContext'
export function ForgotPasswordPage(){const {resetPassword}=useAuth();const [email,setEmail]=useState(''),[sent,setSent]=useState(false),[error,setError]=useState('');async function submit(e:FormEvent){e.preventDefault();setError('');try{await resetPassword(email);setSent(true)}catch(err){setError(err instanceof Error?err.message:'Unable to send reset link.')}}return <main className="center-page auth-simple"><form className="auth-card" onSubmit={submit}><Link to="/login" className="back"><ArrowLeft size={17}/> Back to sign in</Link><p className="eyebrow">ACCOUNT RECOVERY</p><h2>Reset your password</h2><p className="muted">We’ll email you a secure reset link. Your administrator can trigger this too, but can never see your password.</p>{sent?<div className="success">Check your inbox for the reset link.</div>:<><label>Email address<div className="input-wrap"><Mail size={18}/><input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></div></label>{error&&<div className="error">{error}</div>}<button className="button primary">Send reset link</button></>}</form></main>}
