import { ArrowRight, BriefcaseBusiness, CalendarDays, FolderOpen, MapPin, UserRound } from 'lucide-react';import { Link } from 'react-router-dom';import { useAuth } from '../context/AuthContext';import { useAsync } from '../hooks/useAsync';import { dateRange,getContracts,statusLabel } from '../lib/driverData'
const tiles=[['Contracts','View assignments','/contracts',BriefcaseBusiness],['Availability','Plan your schedule','/availability',CalendarDays],['Resources','Tools and guides','/resources',FolderOpen],['Profile','Manage your account','/profile',UserRound]] as const
export function HomePage(){const {profile}=useAuth();const q=useAsync(getContracts,[]);const first=profile?.full_name?.split(' ')[0]||'there',next=q.data?.find(c=>!['approved','bonus_earned','bonus_not_earned'].includes(c.status));return <main className="page"><header className="page-header"><div><p className="eyebrow">ROADSHOW DRIVER</p><h1>Good morning, {first}.</h1><p>Here’s what’s next on your route.</p></div><div className="avatar">{first[0]}</div></header>{next?<section className="show-card"><div className="show-card-top"><div><span className="status-dot"/> NEXT SHOW</div><span className="date-badge">{new Date(`${next.show.starts_on}T12:00:00`).toLocaleDateString(undefined,{month:'short'}).toUpperCase()}<br/><strong>{new Date(`${next.show.starts_on}T12:00:00`).getDate()}</strong></span></div><h2>{next.show.name}</h2><p className="show-meta"><MapPin size={16}/>{next.show.city}{next.show.state?`, ${next.show.state}`:''}<span>•</span>{dateRange(next.show)}</p><div className="progress-label"><strong>{statusLabel(next.status)}</strong></div><Link to={`/contracts/${next.id}`} className="button light">Continue contract <ArrowRight size={18}/></Link></section>:<section className="show-card no-show"><p className="eyebrow">YOU'RE ALL CAUGHT UP</p><h2>No active show assigned</h2><p>Check availability or wait for your next assignment.</p><Link to="/availability" className="button light">Update availability <ArrowRight size={18}/></Link></section>}<section><div className="section-heading"><div><p className="eyebrow">QUICK ACCESS</p><h2>Your workspace</h2></div></div><div className="tile-grid">{tiles.map(([title,sub,to,Icon])=><Link to={to} className="tile" key={title}><span className="tile-icon"><Icon size={22}/></span><span><strong>{title}</strong><small>{sub}</small></span><ArrowRight size={18}/></Link>)}</div></section></main>}
