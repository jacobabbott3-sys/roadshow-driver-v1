import { ArrowRight, BriefcaseBusiness, CalendarDays, FolderOpen, MapPin, UserRound } from 'lucide-react';import { Link } from 'react-router-dom';import { useAuth } from '../context/AuthContext'
const tiles=[['Contracts','View assignments','/contracts',BriefcaseBusiness],['Availability','Plan your schedule','/availability',CalendarDays],['Resources','Tools and guides','/resources',FolderOpen],['Profile','Manage your account','/profile',UserRound]] as const
export function HomePage(){const {profile}=useAuth();const first=profile?.full_name?.split(' ')[0]||'there';return <main className="page"><header className="page-header"><div><p className="eyebrow">ROADSHOW DRIVER</p><h1>Good morning, {first}.</h1><p>Here’s what’s next on your route.</p></div><div className="avatar">{first[0]}</div></header><section className="show-card"><div className="show-card-top"><div><span className="status-dot"/> NEXT SHOW</div><span className="date-badge">DEC<br/><strong>04</strong></span></div><h2>Saratoga Springs</h2><p className="show-meta"><MapPin size={16}/> Saratoga Springs, UT <span>•</span> Dec 4–11</p><div className="progress-label"><strong>Truck loaded</strong><span>72%</span></div><div className="progress"><span style={{width:'72%'}}/></div><Link to="/contracts" className="button light">Continue contract <ArrowRight size={18}/></Link></section><section><div className="section-heading"><div><p className="eyebrow">QUICK ACCESS</p><h2>Your workspace</h2></div></div><div className="tile-grid">{tiles.map(([title,sub,to,Icon])=><Link to={to} className="tile" key={title}><span className="tile-icon"><Icon size={22}/></span><span><strong>{title}</strong><small>{sub}</small></span><ArrowRight size={18}/></Link>)}</div></section></main>}
