/* eslint-disable @typescript-eslint/no-explicit-any */
import { Camera, ClipboardCheck, Plus, Trash2 } from 'lucide-react';import { useState,type FormEvent } from 'react';import { AdminHeader } from '../components/AdminNav';import { PageState } from '../components/PageState';import { useAsync } from '../hooks/useAsync';import { createTemplate,getReviews,getTemplates,reviewContract } from '../lib/adminData'
type DraftSection={title:string;items:{title:string;photo_required:boolean}[]}
export function AdminChecklistsPage(){const templates=useAsync(getTemplates,[]),reviews=useAsync(getReviews,[]);const [name,setName]=useState(''),[kind,setKind]=useState<'setup'|'teardown'>('setup'),[sections,setSections]=useState<DraftSection[]>([{title:'Load Truck',items:[{title:'',photo_required:false}]}]),[creating,setCreating]=useState(false),[busy,setBusy]=useState('');function updateSection(index:number,patch:Partial<DraftSection>){setSections(sections.map((s,i)=>i===index?{...s,...patch}:s))}async function save(e:FormEvent){e.preventDefault();setBusy('create');await createTemplate(name,kind,sections.map(s=>({...s,items:s.items.filter(i=>i.title.trim())})));setName('');setSections([{title:'Load Truck',items:[{title:'',photo_required:false}]}]);setCreating(false);await templates.refresh();setBusy('')}async function review(id:string,approved:boolean){setBusy(id);await reviewContract(id,approved,'');await reviews.refresh();setBusy('')}return <main className="page"><AdminHeader eyebrow="WORKFLOWS" title="Checklists" description="Build reusable setup and teardown instructions, then review submissions."/><div className="admin-actions"><button className="button primary" onClick={()=>setCreating(!creating)}><Plus/> New template</button></div>{creating&&<form className="admin-form checklist-builder" onSubmit={save}><h2>Checklist template</h2><div className="form-grid"><label>Template name<input required value={name} onChange={e=>setName(e.target.value)}/></label><label>Contract type<select value={kind} onChange={e=>setKind(e.target.value as 'setup'|'teardown')}><option value="setup">Setup</option><option value="teardown">Teardown</option></select></label></div>{sections.map((section,si)=><div className="builder-section" key={si}><div className="builder-section-title"><input required value={section.title} onChange={e=>updateSection(si,{title:e.target.value})} placeholder="Section title"/>{sections.length>1&&<button type="button" onClick={()=>setSections(sections.filter((_,i)=>i!==si))}><Trash2/></button>}</div>{section.items.map((item,ii)=><div className="builder-item" key={ii}><input required value={item.title} onChange={e=>updateSection(si,{items:section.items.map((x,i)=>i===ii?{...x,title:e.target.value}:x)})} placeholder="Checklist item"/><label><input type="checkbox" checked={item.photo_required} onChange={e=>updateSection(si,{items:section.items.map((x,i)=>i===ii?{...x,photo_required:e.target.checked}:x)})}/><Camera/> Photo</label></div>)}<button type="button" className="text-button" onClick={()=>updateSection(si,{items:[...section.items,{title:'',photo_required:false}]})}>+ Add item</button></div>)}<button type="button" className="text-button" onClick={()=>setSections([...sections,{title:'',items:[{title:'',photo_required:false}]}])}>+ Add section</button><button className="button primary" disabled={busy==='create'}>Save template</button></form>}<section className="admin-section"><div className="section-row"><div><p className="eyebrow">TEMPLATES</p><h2>Reusable checklists</h2></div></div><PageState loading={templates.loading} error={templates.error}>{!templates.data?.length?<div className="inline-empty">Create your first checklist template above.</div>:templates.data.map((t:any)=><article className="template-row" key={t.id}><ClipboardCheck/><div><strong>{t.name}</strong><span>{t.kind} · v{t.version} · {t.sections?.length||0} sections</span></div><span className="status status-approved">{t.active?'Active':'Inactive'}</span></article>)}</PageState></section><section className="admin-section"><div className="section-row"><div><p className="eyebrow">SUBMISSIONS</p><h2>Awaiting review</h2></div></div><PageState loading={reviews.loading} error={reviews.error}>{!reviews.data?.length?<div className="inline-empty">No driver submissions are waiting.</div>:reviews.data.map(r=><article className="review-row" key={r.id}><div><strong>{r.show.name} · {r.kind}</strong><span>{r.driver?.full_name||'Unassigned'}</span></div><div><button onClick={()=>void review(r.id,false)} disabled={busy===r.id}>Needs work</button><button className="approve" onClick={()=>void review(r.id,true)} disabled={busy===r.id}>Approve</button></div></article>)}</PageState></section></main>}
