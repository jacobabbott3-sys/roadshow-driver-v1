import { ClipboardCheck, FileClock, MessageSquareText, Store as TentTree, UsersRound } from 'lucide-react';import { Link } from 'react-router-dom';import { AdminHeader } from '../components/AdminNav';import { PageState } from '../components/PageState';import { useAsync } from '../hooks/useAsync';import { getDashboardStats,getReviews,reviewContract } from '../lib/adminData';import { dateRange } from '../lib/driverData';import { useState } from 'react'
export function AdminPage(){const stats=useAsync(getDashboardStats,[]),reviews=useAsync(getReviews,[]);const [busy,setBusy]=useState('');async function review(id:string,approved:boolean){setBusy(id);await reviewContract(id,approved,'');await Promise.all([reviews.refresh(),stats.refresh()]);setBusy('')}return <main className="page"><AdminHeader eyebrow="ADMIN WORKSPACE" title="Operations overview" description="Shows, drivers, and work waiting for your attention."/><PageState loading={stats.loading} error={stats.error}><div className="stats admin-stats"><Link to="/admin/shows"><TentTree/><strong>{stats.data?.shows}</strong><span>Upcoming shows</span></Link><article><FileClock/><strong>{stats.data?.unsigned}</strong><span>Awaiting signature</span></article><article><ClipboardCheck/><strong>{stats.data?.reviews}</strong><span>Awaiting review</span></article><Link to="/admin/users"><UsersRound/><strong>{stats.data?.drivers}</strong><span>Active drivers</span></Link><Link to="/admin/operations"><MessageSquareText/><strong>{stats.data?.feedback}</strong><span>New feedback</span></Link></div></PageState><section className="admin-section"><div className="section-row"><div><p className="eyebrow">ACTION NEEDED</p><h2>Checklist reviews</h2></div><Link to="/admin/checklists">Manage templates →</Link></div><PageState loading={reviews.loading} error={reviews.error}>{!reviews.data?.length?<div className="inline-empty">No checklists are waiting for review.</div>:reviews.data.map(r=><article className="review-row" key={r.id}><div><strong>{r.show.name} · {r.kind}</strong><span>{r.driver?.full_name||'Unassigned'} · {dateRange(r.show)}</span></div><div><button disabled={busy===r.id} onClick={()=>void review(r.id,false)}>Needs work</button><button className="approve" disabled={busy===r.id} onClick={()=>void review(r.id,true)}>Approve</button></div></article>)}</PageState></section></main>}
