import { ArrowRight, BriefcaseBusiness, MapPin } from 'lucide-react';import { Link } from 'react-router-dom';import { PageState } from '../components/PageState';import { useAsync } from '../hooks/useAsync';import { dateRange,getContracts,statusLabel } from '../lib/driverData'
export function ContractsPage(){const q=useAsync(getContracts,[]);return <main className="page"><header className="page-header"><div><p className="eyebrow">YOUR WORK</p><h1>Contracts</h1><p>Assignments, progress, and completed shows.</p></div></header><PageState loading={q.loading} error={q.error} empty={!q.data?.length}><div className="contract-list">{q.data?.map(c=><Link className="contract-card" to={`/contracts/${c.id}`} key={c.id}><div className="contract-card-icon"><BriefcaseBusiness/></div><div className="contract-card-body"><div className="card-line"><span className={`status status-${c.status}`}>{statusLabel(c.status)}</span><span>{c.kind}</span></div><h2>{c.show.name}</h2><p><MapPin size={15}/>{c.show.city}{c.show.state?`, ${c.show.state}`:''} · {dateRange(c.show)}</p></div><ArrowRight/></Link>)}</div></PageState></main>}
