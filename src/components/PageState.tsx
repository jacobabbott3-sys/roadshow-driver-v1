import { AlertCircle, Inbox } from 'lucide-react';import { LoadingScreen } from './LoadingScreen'
export function PageState({loading,error,empty,children}:{loading:boolean;error:string;empty?:boolean;children:React.ReactNode}){if(loading)return <LoadingScreen/>;if(error)return <section className="empty-state"><span><AlertCircle/></span><h2>We hit a roadblock</h2><p>{error}</p></section>;if(empty)return <section className="empty-state"><span><Inbox/></span><h2>Nothing here yet</h2><p>Your administrator hasn’t added anything to this area yet.</p></section>;return <>{children}</>}
