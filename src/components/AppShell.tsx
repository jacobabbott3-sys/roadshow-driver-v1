import { BriefcaseBusiness, CircleUserRound, FolderOpen, House, ShieldCheck } from 'lucide-react'; import { NavLink, Outlet } from 'react-router-dom'; import { useAuth } from '../context/AuthContext'
const base=[{to:'/',label:'Home',icon:House},{to:'/contracts',label:'Contracts',icon:BriefcaseBusiness},{to:'/resources',label:'Resources',icon:FolderOpen}]
export function AppShell() { const {profile}=useAuth(); const nav=profile?.role==='admin'?[...base,{to:'/admin',label:'Admin',icon:ShieldCheck},{to:'/profile',label:'Profile',icon:CircleUserRound}]:[...base,{to:'/profile',label:'Profile',icon:CircleUserRound}]; return <div className="app-shell"><aside className="side-nav"><div className="brand"><span className="brand-mark">R</span><span>Roadshow<br/><strong>Driver</strong></span></div><nav>{nav.map(({to,label,icon:Icon})=><NavLink key={to} to={to} end={to==='/'}><Icon size={20}/><span>{label}</span></NavLink>)}</nav><p className="role-pill">{profile?.role==='admin'?'Admin access':'Driver account'}</p></aside><div className="app-main"><Outlet/></div><nav className="bottom-nav">{nav.map(({to,label,icon:Icon})=><NavLink key={to} to={to} end={to==='/'}><Icon size={21}/><span>{label}</span></NavLink>)}</nav></div> }
